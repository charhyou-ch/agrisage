[build]
builder = "railpack"

[deploy]
startCommand = "python agrisage_conseil_server.py"
healthcheckPath = "e6a4"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
